# Hisham Khashman - Portfolio

# deplopyed through vercel, live on:
# https://hishamkhashman.dev

## AI assistant

The floating assistant is now served by the standalone FastAPI app in `assistant-api/`.

Optional environment variables:

- `NEXT_PUBLIC_ASSISTANT_API_URL` - browser-visible URL for the FastAPI service, for example `http://localhost:8000/assistant`
